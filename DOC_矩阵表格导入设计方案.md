# Excel 矩阵表格导入设计方案

## 一、设计目标

将矩阵表格（二维表格）处理从现有流程中**完全独立**出来，形成两条平行的处理路径：

| 特性 | 一维表格（现有） | 矩阵表格（新增） |
|------|------------------|------------------|
| 数据结构 | 行列分明，无合并 | 行列都有维度，需要选择 |
| 表头 | 只有水平表头 | 水平和垂直表头 |
| 字段生成 | 简单的列→字段映射 | 需要行列组合生成字段 |
| 用户交互 | 选表头行即可 | 需要选行表头、列表头 |

---

## 二、架构设计

### 2.1 整体流程图

```
                    ┌─────────────────┐
                    │   步骤1: 上传文件 │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │   步骤2: 选择类型 │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │                              │
     ┌────────▼────────┐           ┌────────▼────────┐
     │    一维表格       │           │    矩阵表格      │
     │   (原有逻辑)     │           │   (全新逻辑)     │
     └────────┬────────┘           └────────┬────────┘
              │                              │
     ┌────────▼────────┐           ┌────────▼────────┐
     │ 选择表头行       │           │ 选择行表头、列表头│
     └────────┬────────┘           └────────┬────────┘
              │                              │
     ┌────────▼────────┐           ┌────────▼────────┐
     │ 调整字段        │           │  预览+调整字段   │
     └────────┬────────┘           └────────┬────────┘
              │                              │
     ┌────────▼────────┐           ┌────────▼────────┐
     │ 步骤3: 创建模板  │           │  步骤3: 创建模板 │
     └─────────────────┘           └─────────────────┘
```

### 2.2 文件结构

```
后端 (kflower-backend/app/)
├── api/v1/endpoints/
│   ├── import_.py          # 现有导入API（保持不变）
│   └── import_matrix.py    # 新增：矩阵表格导入API
├── services/
│   ├── import_service.py   # 现有导入服务（保持不变）
│   └── import_matrix_service.py  # 新增：矩阵表格导入服务
└── schemas/
    └── import_matrix.py    # 新增：矩阵表格导入Schema

前端 (kflower-frontend/src/pc/views/)
└── Templates.vue           # 修改：增加矩阵表格选项卡
```

---

## 三、详细设计

### 3.1 前端设计

#### 步骤2 界面新增

在现有"预览数据"步骤中，在上传完成后首先显示**类型选择**：

```
┌─────────────────────────────────────────────────────────────┐
│  已识别 X 行 × Y 列                                          │
│                                                              │
│  表格类型：                                                    │
│  ┌──────────────┐  ┌──────────────┐                        │
│  │   一维表格    │  │   矩阵表格    │                        │
│  │   ○           │  │   ○           │                        │
│  │  行列分明     │  │  行列都有维度 │                        │
│  └──────────────┘  └──────────────┘                        │
│                                                              │
│  [说明]                                                       │
│  • 一维表格：第一行是表头，每列对应一个字段（默认）               │
│  • 矩阵表格：第一列和第一行都是维度，如报表、交叉表等           │
└─────────────────────────────────────────────────────────────┘
```

#### 矩阵表格选择界面

```
┌─────────────────────────────────────────────────────────────┐
│  矩阵表格配置                                                  │
│                                                              │
│  行表头（垂直维度）：                                           │
│  ┌─────────────────────────────────────┐                     │
│  │ ○ 第 1 行： 产品A | 产品B | 产品C    │  ← 选择这一行       │
│  │ ○ 第 2 行： 类别1  | 类别2  | 类别3  │                     │
│  │ ○ 第 3 行： 季度1  | 季度2  | ...    │                     │
│  └─────────────────────────────────────┘                     │
│                                                              │
│  列表头（水平维度）：                                           │
│  ┌─────────────────────────────────────┐                     │
│  │ ○ 第 1 列： 产品A | 类别1 | Q1 | 100│  ← 选择这一列       │
│  │ ○ 第 2 列： 产品A | 类别1 | Q2 | 150│                     │
│  │ ○ 第 3 列： 产品B | 类别2 | Q1 | 200│                     │
│  └─────────────────────────────────────┘                     │
│                                                              │
│  预览：                                                        │
│  ┌─────────────────────────────────────┐                     │
│  │         │ Q1    | Q2    | Q3       │ ← 列维度             │
│  │  产品A   │ 100   | 150   | 120      │                     │
│  │  产品B   │ 200   | 180   | 220      │ ← 行维度             │
│  └─────────────────────────────────────┘                     │
│                                                              │
│  生成字段预览：                                                 │
│  • 产品A_Q1  (数字)                                           │
│  • 产品A_Q2  (数字)                                           │
│  • 产品A_Q3  (数字)                                           │
│  • 产品B_Q1  (数字)                                           │
│  ...                                                          │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 后端 API 设计

#### 新增端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/import/matrix/parse` | POST | 解析矩阵表格（返回行列维度候选） |
| `/import/matrix/apply-header` | POST | 应用行列表头，生成字段 |
| `/import/matrix/preview` | POST | 预览矩阵表格转换结果 |

#### API 详细设计

**1. /import/matrix/parse**

```python
class MatrixParseRequest(BaseModel):
    all_rows: List[List[str]]      # 所有行数据
    max_row_headers: int = 10      # 最大行表头候选数
    max_col_headers: int = 10      # 最大列表头候选数

class MatrixParseResponse(BaseModel):
    row_header_candidates: List[RowHeaderCandidate]  # 行表头候选
    col_header_candidates: List[ColHeaderCandidate]  # 列表头候选
    detected_row_header: int           # 自动检测的行表头索引
    detected_col_header: int           # 自动检测的列表头索引
    matrix_data: List[List[str]]       # 交叉区域数据
    preview: MatrixPreview              # 预览信息
```

**2. /import/matrix/apply-header**

```python
class MatrixApplyRequest(BaseModel):
    all_rows: List[List[str]]
    row_header_row: int        # 行表头所在行
    col_header_col: int       # 列表头所在列
    merge_type: str = "concat"  # 组合方式：concat/underline/none

class MatrixApplyResponse(BaseModel):
    row_headers: List[str]      # 展开后的行表头 ["产品A_Q1", "产品A_Q2", ...]
    col_headers: List[str]      # 展开后的列表头（用于显示）
    headers: List[str]          # 最终表头（与行表头相同）
    rows: List[List[str]]       # 转换后的数据行
    fields: List[dict]          # 生成的字段定义
    total_rows: int
    total_columns: int
```

### 3.3 数据转换算法

#### 矩阵转一维的核心算法

```
输入：
┌─────────┬────────┬────────┬────────┐
│         │  Q1   │  Q2   │  Q3   │  ← 列维度（用户选择第0行）
├─────────┼────────┼────────┼────────┤
│ 产品A   │  100   │  150   │  120   │
│ 产品B   │  200   │  180   │  220   │  ← 行维度（用户选择第0列）
├─────────┼────────┼────────┼────────┤
│ 总计    │  300   │  330   │  340   │
└─────────┴────────┴────────┴────────┘

输出（一维表格）：
┌──────────┬────────┬────────┐
│ 产品     │ 季度   │ 数值   │
├──────────┼────────┼────────┤
│ 产品A    │ Q1     │ 100    │
│ 产品A    │ Q2     │ 150    │
│ 产品A    │ Q3     │ 120    │
│ 产品B    │ Q1     │ 200    │
│ 产品B    │ Q2     │ 180    │
│ 产品B    │ Q3     │ 220    │
│ 总计     │ Q1     │ 300    │
│ 总计     │ Q2     │ 330    │
│ 总计     │ Q3     │ 340    │
└──────────┴────────┴────────┘

字段生成：
- 产品 (text)
- 季度 (text)
- 数值 (number)
```

#### 字段组合规则

| 组合方式 | 行表头 | 列表头 | 结果示例 |
|---------|--------|--------|---------|
| concat（默认） | 产品A | Q1 | 产品A_Q1 |
| underline | 产品A | Q1 | 产品A_Q1 |
| none | 产品A | Q1 | Q1（只用列表头）|

---

## 四、字段设计

### 4.1 字段生成规则

```python
def generate_matrix_fields(
    row_headers: List[str],   # 展开后的行表头
    col_headers: List[str],    # 展开后的列表头
    data_sample: List          # 数据样本用于类型推断
) -> List[dict]:
    """生成矩阵表格的字段定义"""
    fields = []

    # 1. 行维度字段
    fields.append({
        "name": "row_dimension",
        "label": row_dim_label,  # 如"产品"
        "type": "text",
        "required": True,
        "width": "100%"
    })

    # 2. 列维度字段（如果用户选择保留）
    if keep_col_dimension:
        fields.append({
            "name": "col_dimension",
            "label": col_dim_label,  # 如"季度"
            "type": "select",        # 自动推断为选择类型
            "options": col_headers,
            "required": True,
            "width": "50%"
        })

    # 3. 数值字段（每个组合一个）
    for i, row_h in enumerate(row_headers):
        fields.append({
            "name": f"value_{i}",
            "label": row_h,          # 显示名称
            "type": infer_type(data_sample[i]),  # 自动推断
            "required": False,
            "width": "50%"
        })

    return fields
```

### 4.2 数据存储结构

矩阵表格转换后存储为标准一维结构，与现有系统完全兼容：

```json
{
  "name": "销售报表矩阵",
  "modules": [{
    "name": "main",
    "label": "主表单",
    "fields": [
      {"name": "产品", "label": "产品", "type": "text"},
      {"name": "季度", "label": "季度", "type": "select", "options": ["Q1","Q2","Q3","Q4"]},
      {"name": "销售额", "label": "销售额", "type": "money"}
    ]
  }]
}
```

---

## 五、实现计划

### 5.1 第一阶段：核心功能（矩阵表格专用流程）

| 任务 | 文件 | 说明 |
|------|------|------|
| 1.1 创建 Schema | `schemas/import_matrix.py` | 定义请求/响应模型 |
| 1.2 创建 Service | `services/import_matrix_service.py` | 核心转换算法 |
| 1.3 创建 API | `endpoints/import_matrix.py` | 三个 API 端点 |
| 1.4 注册路由 | `api/v1/api.py` | 挂载新路由 |
| 1.5 前端选项卡 | `Templates.vue` | 类型选择界面 |

### 5.2 第二阶段：界面集成

| 任务 | 说明 |
|------|------|
| 2.1 矩阵表格选择组件 | 行表头/列表头下拉选择 |
| 2.2 预览组件 | 矩阵→一维预览 |
| 2.3 字段调整界面 | 复用现有界面 |

### 5.3 第三阶段：增强功能

| 任务 | 说明 |
|------|------|
| 3.1 智能检测 | 自动识别矩阵表格特征 |
| 3.2 合并单元格处理 | 处理跨行跨列的表头 |
| 3.3 行列维度标签 | 用户自定义维度名称 |

---

## 六、关键问题处理

### 6.1 空单元格处理
- 空单元格在转换后生成一行，但数值为空
- 前端可以选择"跳过空行"

### 6.2 行列维度数量
- 建议限制：行表头 ≤ 50 个唯一值
- 建议限制：列表头 ≤ 20 个唯一值
- 超限提示用户简化表格结构

### 6.3 合并单元格处理
- 读取时保留合并信息
- 转换时将合并的维度值展开到每行

---

## 七、待定问题（实现时再讨论）

1. 行表头和列表头如何选择？
   - 方案A：下拉框选择行号/列号
   - 方案B：点击表格行/列自动选择

2. 多行/多列表头如何处理？
   - 是否支持多行垂直合并的表头
   - 如何确定维度的层级关系

3. 维度标签的自定义
   - 用户是否可以自定义"产品"这个维度名称
   - 还是自动从第一行/列推断

---

*创建时间：2026-05-03*
*状态：设计方案，待实现*
