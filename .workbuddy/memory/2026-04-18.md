# 2026-04-18 工作日志

## 新增：模板设计器列表视图（表格+卡片切换）

**需求**：默认列表（表格）展示，可切换卡片展示；表格列包括：模板名称、发布状态、创建人、共享状态、分类、字段数、模板ID、创建时间。

**改动**：

1. **后端** `templates.py` + `schemas.py`：
   - `TemplateResponse` 增加 `created_by` 字段
   - `list_templates`、`get_template`、`create_template`、`update_template` 均返回 `created_by`

2. **前端**（两个 Templates.vue 均改）：
   - 新增 `listStyle` ref，默认 `'table'`
   - 导入 `useUserStore`，计算 `currentUserId`
   - 新增 `getCreatorName(t)`：返回 "我" / "用户ID" / "—"
   - 新增 `formatDate(s)`：精确到分钟的时间格式化
   - 表格视图：`el-table` 含 8 列 + 操作列，点击行打开设计器
   - 卡片视图保留为 `listStyle === 'card'` 分支
   - 表头新增切换按钮 `el-button-group`（列表 / 卡片）
   - 表格样式：`.template-table-wrapper` + `.table-name-cell` 等

## 新增：表格操作列显式按钮 + 发布动态建表功能

**需求**：
1. 列表操作列从三个点 dropdown 改为显式增删改按钮
2. 模板编码用 ID 自动生成，移除用户输入
3. 点发布时创建数据库表 + 表单/列表/详情页面
4. 工作区独立展示模板列表和表单列表

**改动**：

1. **后端** `templates.py`：
   - `create_template`: 编码自动生成 `form_{id}`，创建后更新
   - `publish_template`: 动态解析 modules 字段，构建 CREATE TABLE 语句，创建表后保存表名到 config.table_name
   - `submit_template_data`: 改为 INSERT 到动态表
   - `get_template_data_list`: 改为 SELECT * FROM 动态表
   - `get_template_data_detail`: 改为 SELECT * FROM 动态表
   - `update_template_data`: 改为 UPDATE 动态表
   - `delete_template_data`: 改为 DELETE FROM 动态表

2. **前端 Templates.vue**（两个均改）：
   - 移除 `@row-click="openDesigner"` 行点击
   - 操作列宽度 120→280，改为4个显式按钮：查看、编辑、发布/填表、删除
   - 移除编辑弹窗的"模板编码"输入，改为显示"模板ID"（disabled）
   - 新增 `openFormSubmit()` 函数

3. **前端 MyWorkspace.vue**：
   - 完全重构，格式参照 Templates.vue 表格视图
   - "我的模板"：增删改查 + 发布/填表 + 撤回
   - "我发布的表单"：填表、列表、编辑、撤回
   - 新增搜索防抖、字段数量、模板颜色、创建人显示
