# 2026-04-29 工作日志

- **修复 AI辅助设计 500 错误**：根因是 openai 1.12.0 与 httpx 0.28.1 不兼容（httpx 0.28 移除了 `proxies` 参数，改用 `proxy`，但旧版 openai 库内部仍传 `proxies` 给 httpx）。解决方案：升级 openai 包从 1.12.0 → 2.33.0，与 httpx 0.28.1 完全兼容。代码无需修改，`AsyncOpenAI` 用法在 v2 中完全向后兼容。
